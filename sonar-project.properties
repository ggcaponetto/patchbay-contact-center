# SonarQube Cloud configuration - the scan runs in CI (ci.yml) once the
# SONAR_TOKEN repo secret exists.
# The key was minted when the repo was still "livekit-playground"; SonarCloud keys are
# immutable, so it stays until the project is re-imported under the new name.
sonar.projectKey=ggcaponetto_livekit-playground
sonar.organization=ggcaponetto
sonar.projectName=patchbay-contact-center

sonar.sources=apps/agent/src,apps/api/src,apps/web/src,apps/embed/src,packages/shared/src
sonar.tests=apps
sonar.test.inclusions=**/*.test.ts,**/*.test.tsx
sonar.exclusions=docs/**,**/dist/**,**/coverage/**,apps/api/drizzle/**
sonar.javascript.lcov.reportPaths=coverage/lcov.info

# Same contract as vitest.config.ts: every source file is covered; only tests are excluded.
sonar.coverage.exclusions=**/*.test.ts,**/*.test.tsx

sonar.sourceEncoding=UTF-8
