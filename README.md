# Contact Center POC

An AI-first, multi-tenant contact center proof of concept built on [LiveKit Cloud](https://cloud.livekit.io/).

- End customers press an embeddable **"Call us"** web component on any website and talk over WebRTC.
- An **AI voice agent** answers first (configurable per tenant: `ai-first` or `human-first`) and escalates to a human when asked.
- **Human agents and supervisors** work from a React + MUI desk, signed in with Google via Better Auth.
- **Routing** (queues, availability, ring one agent at a time) and a **supervisor dashboard** (live calls, settings, listen-in / take-over, history).
- Every conversation (full transcript, AI and human segments, call events) is stored in **Postgres**, ready for an LLM to act on later.

## Documentation

The developer documentation is a VitePress site built from this repository: `npm run docs:dev` (or the published site from the `Docs` workflow).

| Start here                                                  | Then                                                                                                                                                                                        |
| ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [Getting started](docs/guide/getting-started.md)            | [Architecture](docs/guide/architecture.md) · [Call lifecycle](docs/guide/call-lifecycle.md)                                                                                                 |
| [Testing](docs/guide/testing.md)                            | [Quality gates](docs/guide/quality-gates.md) · [Deployment](docs/guide/deployment.md)                                                                                                       |
| Per-app docs live next to the code (`README.md` per folder) | [API](apps/api/README.md) · [Agent](apps/agent/README.md) · [Web desk](apps/web/README.md) · [Embed](apps/embed/README.md) · [Shared](packages/shared/README.md) · [Tests](tests/README.md) |

The API reference (`docs/api`) is generated by typedoc from the TSDoc comments and is part of `npm run docs:build`; undocumented exports fail the build.

## Workspaces

| Path              | What                                                                                                    |
| ----------------- | ------------------------------------------------------------------------------------------------------- |
| `apps/agent`      | LiveKit Agents worker (Node.js). Voice pipeline on LiveKit Inference; tools for escalation and hang-up  |
| `apps/api`        | Fastify API: Better Auth (Google), LiveKit tokens & dispatch, routing, WebSocket for the desk, Postgres |
| `apps/web`        | Vite + React + MUI desk for agents and supervisors                                                      |
| `apps/embed`      | `<cc-call-button>` web component, built as a single `call-button.js` script                             |
| `packages/shared` | zod schemas and types shared by every app (tenant settings, call status, WS messages)                   |
| `tests`           | Playwright end-to-end (`tests/e2e`) and Artillery load (`tests/load`) suites                            |

## Quick start

```console
npm install
cp .env.example .env.local            # then fill in the values (see Getting started)
lk cloud auth && lk app env -w -d .env.local
docker compose up -d                  # Postgres
npm run dev:api                       # http://localhost:4000
npm run dev:web                       # http://localhost:3000
npm run dev:agent                     # registers the AI agent with LiveKit Cloud
npm run dev:embed                     # http://localhost:3001 demo page with the call button
```

No Google OAuth client yet? Set `DEV_USER_EMAIL=you@example.com` (and the same address in `ADMIN_EMAILS`) to be signed in automatically during development.

## Tests and quality gates

| Command                    | What                                                                                   |
| -------------------------- | -------------------------------------------------------------------------------------- |
| `npm test`                 | unit + integration tests with the 90 % coverage gate (integration needs Postgres)      |
| `npm run test:unit`        | unit tests only (`*.test.ts` next to the sources)                                      |
| `npm run test:integration` | `*.integration.test.ts` (Postgres, LiveKit Inference evals)                            |
| `npm run test:e2e`         | Playwright against the real stack, including LiveKit Cloud (opt-in)                    |
| `npm run test:load`        | Artillery against a running API on :4100 (opt-in)                                      |
| `npm run validate`         | everything CI runs: prettier, eslint, tsc, knip, cspell, LOC gate, tests, builds, docs |

Hard size budget: 50k non-blank source lines (`npm run loc`), POC target under 20k.

## License

MIT — see the `LICENSE` file.
