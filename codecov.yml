# The ENFORCED coverage gate is vitest's 90%-on-every-metric threshold
# (vitest.config.ts), run in `npm test` on every OS and blocking via
# `validate`. Codecov is the trend tracker and badge, not a second gate.
coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true
